# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Playwright auth fixtures (storageState) — never commit
e2e/.auth/

# Test coverage output
coverage/

# Vendored impeccable agent-skill tooling (installed per-machine, not project source)
.claude/skills/impeccable/
.cursor/skills/
.cursor/hooks.json
.github/skills/
.github/hooks/

# impeccable live-mode local session/recovery state (keep design.json + live/config.json)
.impeccable/live/sessions/
